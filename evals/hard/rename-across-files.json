{
  "id": "rename-across-files",
  "prompt": "This package has orders.py (defining functions `total` and `total_summary`) and main.py. Rename ONLY the function `total` to `grand_total` everywhere it is defined and called, across every file. Do NOT rename `total_summary` \u2014 it must keep its name and keep working \u2014 and do not change any string literals. `python3 main.py` must still run and print exactly the same two lines it prints now.",
  "setup": "cat > orders.py <<'FEOF'\ndef total(items):\n    return sum(i[\"price\"] * i[\"qty\"] for i in items)\n\ndef total_summary(items):\n    return f\"{len(items)} items, total={total(items)}\"\nFEOF\ncat > main.py <<'FEOF'\nfrom orders import total, total_summary\n\nitems = [{\"price\": 3, \"qty\": 2}, {\"price\": 5, \"qty\": 1}]\nprint(total(items))\nprint(total_summary(items))\nFEOF",
  "check": "python3 - <<'PYEOF'\nimport subprocess, pathlib\nout = subprocess.run([\"python3\", \"main.py\"], capture_output=True, text=True)\nassert out.returncode == 0, out.stderr\nassert out.stdout.strip().splitlines() == [\"11\", \"2 items, total=11\"], repr(out.stdout)\norders = pathlib.Path(\"orders.py\").read_text()\nmain = pathlib.Path(\"main.py\").read_text()\nassert \"def grand_total(\" in orders, \"total not renamed in its definition\"\nassert \"def total(\" not in orders, \"old total definition still present\"\nassert \"def total_summary(\" in orders, \"total_summary must be left alone\"\nassert \"grand_total_summary\" not in orders and \"grand_total_summary\" not in main, \"collision: total_summary got renamed\"\nprint(\"ok\")\nPYEOF",
  "timeoutSeconds": 300
}

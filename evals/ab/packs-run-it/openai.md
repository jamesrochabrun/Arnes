Call tools with exact JSON arguments. Prefer rewriting whole functions over
fragile partial edits. Keep answers terse.

Before you finish, run the thing you built against the input the task names and
read the output. A syntax or compile check — `py_compile`, `bash -n`,
`node --check` — proves the file parses, not that it is right, so it is never
the last thing you do.

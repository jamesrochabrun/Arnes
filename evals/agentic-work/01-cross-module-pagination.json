{
  "id": "cross-module-pagination",
  "prompt": "Fix the pagination bug in this Python package. The public page(items, offset, limit) function must return up to limit items starting at offset, and next_offset must be the next unread position or None at the end. Negative offset or nonpositive limit must raise ValueError. Keep the function signature and result keys unchanged. Add regression tests for edge cases and run the tests. Do not modify fixtures.txt.",
  "setup": "mkdir -p pager; printf '%s\n' 'def bounds(size, offset, limit):' '  return offset, min(size, limit)' > pager/bounds.py; printf '%s\n' 'from .bounds import bounds' '' 'def page(items, offset, limit):' '  start, stop = bounds(len(items), offset, limit)' '  return {\"items\": items[start:stop], \"next_offset\": stop if stop < len(items) else None}' > pager/__init__.py; printf '%s\n' 'Do not alter this input fixture.' > fixtures.txt",
  "check": "python3 - <<'PY'\nfrom pager import page\nfrom pathlib import Path\nassert Path('fixtures.txt').read_text() == 'Do not alter this input fixture.\\n'\nfor size in range(0, 19):\n  items = list(range(size))\n  original = items.copy()\n  for offset in range(0, size + 3):\n    for limit in (1, 2, 5, 20):\n      expected = items[offset:offset + limit]\n      stop = min(size, offset + limit)\n      result = page(items, offset, limit)\n      assert result == {'items': expected, 'next_offset': stop if stop < size else None}, (size, offset, limit, result)\n      assert items == original\nfor offset, limit in ((-1, 2), (0, 0), (0, -2)):\n  try:\n    page([1, 2], offset, limit)\n  except ValueError:\n    pass\n  else:\n    raise AssertionError('invalid bounds accepted')\nassert list(Path('.').glob('**/test*.py')), 'add regression tests'\nPY",
  "limits": {"maxSteps": 60, "maxCostUSD": 3}
}
